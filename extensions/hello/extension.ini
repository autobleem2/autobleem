[extension]
Name=Hello
Description=The SDK's sample extension
Author=AutoBleem team
Version=1.0.0
# resolved by AppManifest's rule; .so / .dll is added per platform
Plugin=bin/{key}/hello
# loaded at start-up and polled every frame: it shows a bubble for a few seconds
Background=true
Network=none
